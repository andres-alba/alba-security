import 'package:flutter/material.dart';

class TimeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Time Screen'),
    );
  }
}
